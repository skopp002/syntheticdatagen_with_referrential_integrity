from setuptools import setup, find_packages
setup(
    name='nested_lookup',
    version='0.2.25',  # Use the version you have installed
    packages=find_packages(),
    install_requires=[],  # Add any known dependencies here
)